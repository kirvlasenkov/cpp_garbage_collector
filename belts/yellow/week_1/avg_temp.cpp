#include <iostream>
#include <vector>

/*
int main() {

    int32_t N;
    std::vector<int32_t> temp_folder;
    std::cin >> N;
    int64_t sum = 0;

    for (int32_t i = 0; i < N; ++i) {
        int64_t temp;
        std::cin >> temp;
        temp_folder.push_back(temp);
        sum += temp;
    }

    int64_t avg = sum / static_cast<int>(temp_folder.size());

    std::vector<int32_t> bigger_avg;
    for (uint32_t i = 0; i < N; i++) {
        if (temp_folder[i] > avg) {
            bigger_avg.push_back(i);
        }
    }

    std::cout << bigger_avg.size() << std::endl;
    for (const auto &elem: bigger_avg) {
        std::cout << elem << " ";
    }
    std::cout << std::endl;
    return 0;
} */
