#include <iostream>
#include <fstream>
#include <iomanip>
#include <vector>

/*int main() {
    std::ifstream input("input.txt");

    if (input) {

        std::vector<double> int_container;
        double number;

        for (int i = 0; i <= 2; i++) {
            input >> number;
            int_container.push_back(number);

        }

        for (const auto &elem: int_container) {

            std::cout << std::fixed << std::setprecision(3);
            std::cout << elem << std::endl;
        }
    }

    return 0;
}*/